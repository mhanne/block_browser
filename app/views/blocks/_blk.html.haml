%td
  %a{:href => block_path(blk.hash)}= blk.depth
%td
  %a{:href => block_path(blk.hash)}= blk.hash
%td= format_time blk.time
%td= blk.tx.size
%td= number_to_human_size blk.to_payload.bytesize
%td.value= format_amount(blk.tx.map(&:out).flatten.map(&:value).sum)
